{
  "name": "rust-wasm-web-ide",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:github": "vite build --mode production && node scripts/copy-github-assets.js",
    "build:cloudflare": "vite build --mode cloudflare && node scripts/copy-cloudflare-assets.js",
    "build:all": "vite build --mode production && vite build --mode cloudflare",
    "deploy:github": "vite build --mode production && git add docs-github && git commit -m 'deploy: github' && git push origin main",
    "deploy:cloudflare": "vite build --mode cloudflare && wrangler pages deploy docs-cloudflare --project-name=rust-wasm-web-ide --commit-dirty=true",
    "deploy:all": "vite build --mode production && vite build --mode cloudflare && git add docs-github && git commit -m 'deploy: github' && git push origin main && wrangler pages deploy docs-cloudflare --project-name=rust-wasm-web-ide --commit-dirty=true",
    "build:check": "vue-tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore"
  },
  "dependencies": {
    "@monaco-editor/loader": "^1.4.0",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0",
    "monaco-editor": "^0.45.0",
    "vue": "^3.4.0",
    "vue-i18n": "^9.9.0",
    "xterm": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "eslint": "^8.0.0",
    "eslint-plugin-vue": "^9.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vue-tsc": "^1.8.0"
  }
}
